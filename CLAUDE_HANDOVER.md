# Project Handover: SQD-APP (Aircraft Maintenance QA System)

This document provides a comprehensive state-of-the-union for the **SQD-APP**, a full-stack Quality Assurance platform for aviation maintenance. Use this as context for Claude Code or any subsequent AI agent.

## 1. Project Objective
Build a robust, role-based system for aircraft maintenance surveillance.
- **Key Features:** Dynamic branching templates, task assignment, finding/audit trail management, and role-based access control (RBAC).
- **Core Workflow:** Admin creates Template -> Manager assigns Task -> Staff executes Form -> Findings are raised -> Follow-up actions are tracked.

## 2. Tech Stack
- **Backend:** Node.js (v20+), Express, TypeScript, Prisma ORM, PostgreSQL.
- **Frontend:** Next.js 15 (App Router), TypeScript, Tailwind CSS v4.0.
- **State Management:** Zustand (with Persistence).
- **Networking:** Axios with JWT interceptors.
- **Testing:** Jest + Supertest (Backend).

## 3. Current Implementation Status

### Phase 1 & 2: Backend Foundation & Security (COMPLETED)
- **Database Schema:** Fully modeled in `backend/prisma/schema.prisma`. Includes Users, Roles, Divisions, Templates, Tasks, Findings, and Audit Logs.
- **Authentication:** JWT-based stateless auth. Passwords hashed with `bcrypt`.
- **RBAC:** Middleware implemented for `Director`, `Admin`, `Manager`, `Group Leader`, and `Staff`.
- **Parity:** `sqd_qa_db` (Dev) and `sqd_qa_test_db` (Test) schemas are kept 100% in sync via Prisma.

### Phase 3 & 4.1: Frontend & App Shell (COMPLETED)
- **Scaffold:** Clean Next.js architecture in `/frontend`.
- **Theme:** Professional Light Theme using Tailwind 4 (`slate-50` backgrounds, `blue-600` primary).
- **Auth UI:** 
    - `/login`: Fully functional, handles `202` (Password Change Required).
    - `/update-password`: Forced flow for new accounts.
    - `/forgot-password`: Mocks email by printing reset tokens to the backend terminal.
- **App Shell:** Persistent Sidebar and Header. Sidebar dynamically hides links based on user `Role`.

### Phase 4.2: Password Management (COMPLETED)
- Implemented `forcePasswordChange` flag logic.
- Implemented backend endpoints for `update-password`, `forgot-password`, and `reset-password`.

### Phase 4.3: JSON Template Builder (COMPLETED)
- **Backend:** Full CRUD API implemented in `template.controller.ts`.
- **Frontend Builder:** Visual drag-and-drop style builder in `/dashboard/templates/new`.
- **Dynamic Data:** `GET /api/datasources` endpoint added to allow dropdowns to pull from DB (Departments, Aircraft, etc.).
- **Revisions:** Revision incrementing and Draft/Publish status logic fully functional.

## 4. Connection & Environment Details
- **Backend Port:** 5000 (Set in `backend/.env`)
- **Frontend Port:** 3000 (Default Next.js)
- **Database:** PostgreSQL on `localhost:5432`.
- **Master User:** `director@sqd.com` / `password123`.
- **JWT Secret:** `super-secret-development-key-12345` (in `.env`).

## 5. Immediate Next Steps for Claude Code
1. **Task Assignment Logic:** Implement the backend and frontend for assigning a `Template` to a `User` or `Division` (creating a `Task` record).
2. **Task Execution UI:** Build the interface for Staff to fill out a `Task` based on the `formSchema`.
3. **Audit Log Integration:** Ensure that when a task status changes, an `AuditLog` entry is created.

## 6. Known "Gotchas"
- **Prisma Generation:** Always run `npx prisma generate` in `/backend` after schema changes to update the types.
- **Port Conflict:** Ensure the backend stays on `5000` to avoid clashing with the Next.js dev server on `3000`.
- **CORS:** Backend `index.ts` is configured with `app.use(cors())`, allowing all origins for local dev.
- **Sidebar Fix:** Corrected a bug in `Sidebar.tsx` where it was looking for `user.role.name` instead of the string `user.role` provided by the API.

---
*Generated by Antigravity AI on 2026-05-07.*
